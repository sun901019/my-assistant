# ═══════════════════════════════════════════════════
# my-assistant 環境變數範本
# 使用方式：
#   1. 複製這個檔案為 .env（不會被推上 git）
#   2. 填入真實的 token / ID
#   3. .env 檔只在本機，絕對不 commit
# ═══════════════════════════════════════════════════

# ─────────── LINE Messaging API ───────────
# 用途：line-notify/line_push.py 推播訊息給老闆
# 來源：LINE Developers Console → Messaging API → Channel access token
LINE_USER_ID=
LINE_ACCESS_TOKEN=
